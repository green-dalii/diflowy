---
const { message, linkText, url } = Astro.props;
---
<div class="w-full flex justify-center">
    <div class="mt-20 rounded-full banner py-2 px-6 w-full max-w-xl text-center shadow-md">
        <p class="text-primary text-xs">
            {message}<span class="ml-2"><a href={url} class="link font-bold">{linkText}</a></span>
        </p>
    </div>
</div>